name: subscription_financial_model
version: "1.0.0"
config-version: 2

profile: subscription_financial_model

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

vars:
  kkbox_start: "2015-01"
  kkbox_end: "2017-03"

models:
  subscription_financial_model:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
